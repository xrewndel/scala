/**
 * Tests for "raw" API implementation, e.g. direct client layer
 */

package tests.axiom

import org.scalatest.FlatSpec
import org.scalatest.matchers.ShouldMatchers

import net.fmb.integrator.serviceprovider.axiom._

class RawAPISpec extends FlatSpec with ShouldMatchers {
  // TODO: tests (with mocked connection data) for Raw API implementation
}