/**
 * Tests for API implementations
 */
package tests.axiom

import org.scalatest.FlatSpec
import org.scalatest.matchers.ShouldMatchers

import collection.JavaConversions._
import collection.JavaConverters._

import net.fmb.integrator.serviceprovider.axiom._

class APISpec extends FlatSpec with ShouldMatchers {
  // TODO: tests for api implementations (derived from AbstractServiceProviderAPI)
}